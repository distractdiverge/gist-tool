export interface Goal {
    id: string;
    name: string;
    description: string;
}

export interface Idea {
    id: string;
    name: string;
    description: string;
}

export interface StepProject {
    id: string;
    name: string;
    description: string;
}

export interface Task {
    id: string;
    name: string;
    description: string;
}